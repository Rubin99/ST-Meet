<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stmeet">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.STMeet">
        <activity android:name=".payment.PaymentEsewaActivity"></activity>
        <activity android:name=".chat.RateTeacherActivity" />
        <activity android:name=".payment.PaymentDetails" />
        <activity android:name=".payment.TeacherPaymentActivity" />
        <activity android:name=".student_requests.StudentRequestActivity" />
        <activity android:name=".java_display.JavaDisplayActivity" />
        <activity android:name=".teacher_info.TeacherInfoActivity" />
        <activity android:name=".SubjectListActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".matches.MatchesActivity" />
        <activity android:name=".info.UserInfoActivity" />
        <activity android:name=".login_registration.LoginActivity" />
        <activity android:name=".login_registration.RegistrationActivity" />
        <activity android:name=".login_registration.ChooseLoginRegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>